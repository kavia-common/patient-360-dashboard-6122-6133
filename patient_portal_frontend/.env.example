# Patient 360 Frontend Environment Variables

# PUBLIC_INTERFACE
# Base URL for backend FastAPI server (e.g., http://localhost:3001 or full https URL)
# Change this if your backend runs on a different host/port.
REACT_APP_BACKEND_URL=http://localhost:3001

# PUBLIC_INTERFACE
# Enable direct Gemini API calls from frontend (default false). If false, uses backend endpoint /chatbot/send
REACT_APP_GEMINI_DIRECT=false

# PUBLIC_INTERFACE
# Optional: API key for direct Gemini mode (only used if REACT_APP_GEMINI_DIRECT=true)
REACT_APP_GEMINI_API_KEY=

# PUBLIC_INTERFACE
# Optional: Gemini model to use in direct mode
REACT_APP_GEMINI_MODEL=gemini-1.5-flash
